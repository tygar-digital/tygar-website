---
const { data } = Astro.props;
const accentBg: Record<string,string> = {
  emerald: 'rgba(94,234,212,.12)', cyan: 'rgba(13,148,136,.12)', violet: 'rgba(19,78,74,.12)',
};
const accentColor: Record<string,string> = {
  emerald: '#5EEAD4', cyan: '#0D9488', violet: '#0F766E',
};
const accentBorder: Record<string,string> = {
  emerald: 'rgba(94,234,212,.3)', cyan: 'rgba(13,148,136,.3)', violet: 'rgba(19,78,74,.3)',
};
const accentGlow: Record<string,string> = {
  emerald: '0 12px 48px rgba(94,234,212,.20),0 0 0 1px rgba(94,234,212,.2)',
  cyan:    '0 12px 48px rgba(13,148,136,.20),0 0 0 1px rgba(13,148,136,.2)',
  violet:  '0 12px 48px rgba(19,78,74,.20),0 0 0 1px rgba(19,78,74,.2)',
};
---
<section id="portfolio" class="border-t border-brand-border">
  <div class="max-w-[1160px] mx-auto px-8 py-[108px]">

    <div class="flex items-center gap-2 text-[0.72rem] font-sans font-bold tracking-[0.12em] uppercase text-brand-secondary mb-3">
      <span class="w-[18px] h-[2px] flex-shrink-0" style="background:linear-gradient(90deg,#0D9488,#0F766E,#134E4A)"></span>
      {data.label}
    </div>
    <h2 class="font-handwritten font-normal leading-[1.08] tracking-[-0.03em] text-brand-heading mb-4"
      style="font-size:clamp(2rem,4.5vw,3.25rem)">
      {data.heading[0]} <span class="bg-clip-text text-transparent"
        style="background-image:linear-gradient(90deg,#0D9488,#0F766E,#134E4A)">{data.heading[1]}</span>
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mt-12">
      {data.items.map((item: any) => (
        <div class="rounded-card border p-8 flex flex-col gap-4 relative overflow-hidden transition-all duration-[280ms] hover:-translate-y-1.5 hover:shadow-card"
          style={`background:rgba(39,39,42,0.55);backdrop-filter:blur(12px);border-color:${accentBorder[item.accent]}`}
          onmouseenter={`this.style.boxShadow='${accentGlow[item.accent]}'`}
          onmouseleave={`this.style.boxShadow='none'`}>

          <!-- Background label -->
          <span class="absolute top-4 right-4 text-5xl font-sans font-bold opacity-[0.04] select-none pointer-events-none"
            style={`color:${accentColor[item.accent]}`}>
            {item.bg_label}
          </span>

          <!-- Icon -->
          <div class="w-10 h-10 rounded-lg flex items-center justify-center flex-shrink-0"
            style={`background:${accentBg[item.accent]};color:${accentColor[item.accent]}`}
            set:html={item.icon_svg}>
          </div>

          <!-- Tags -->
          <div class="flex gap-1.5">
            {item.tags.map((tag: string) => (
              <span class="text-[0.68rem] font-sans font-semibold px-2.5 py-1 rounded-full border"
                style={`background:${accentBg[item.tag_color]};border-color:${accentBorder[item.tag_color]};color:${accentColor[item.tag_color]}`}>
                {tag}
              </span>
            ))}
          </div>

          <h3 class="font-sans font-bold text-brand-heading text-lg">{item.title}</h3>
          <p class="text-brand-secondary text-sm leading-relaxed flex-1">{item.text}</p>

          <!-- Result -->
          <div class="text-sm font-sans font-semibold" style={`color:${accentColor[item.result_color]}`}>
            {item.result}
          </div>
        </div>
      ))}
    </div>

    {data.cta && (
      <div class="text-center mt-10">
        <a href={data.cta.href}
          class="font-sans font-bold text-sm transition-colors hover:underline"
          style={`color:#0F766E`}>
          {data.cta.label}
        </a>
      </div>
    )}
  </div>
</section>
